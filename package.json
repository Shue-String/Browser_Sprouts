{
  "name": "sprouts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:generated && tsc && vite build && vite build --config vite.voronoiTest.config.js",
    "preview": "vite preview",
    "gen:headers": "node scripts/genCollectionElementsHeader.cjs && node scripts/genGenomeDefsHeader.cjs",
    "check:generated": "node scripts/checkGeneratedHeaders.cjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
